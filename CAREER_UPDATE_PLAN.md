# Career repo update plan — ParkCast SF

Copy this file into `~/career/` and run it from that directory in Cursor Agent mode.

**Source of truth for technical details:** ParkCast-SF at `/Users/brandonminer/projects/ParkCast-SF` (or clone from [Brandonminer333/ParkCast-SF](https://github.com/Brandonminer333/ParkCast-SF)).

**Live app:** https://parkcast-sf.vercel.app/

**Do not commit** unless the user explicitly asks.

---

## Scope

| Task | File(s) | Action |
|------|---------|--------|
| GitHub profile README | `GITHUB_README.md` | Add **Live** link to existing Parkcast row only |
| Resume master inventory | `resume/latex/MASTER-RESUME.tex` | Replace active Parkcast bullets; retire old variants |
| Canonical resumes | `resume/scripts/rebuild_canonical_resumes.py` | Update `BUILDS` item IDs; rebuild |
| Generated role `.tex` | `resume/latex/ai-engineer.tex`, `resume/latex/forward-deployed-engineer.tex` | Regenerated by rebuild script |

**Out of scope for this plan:** `portfolio/lib/projects.ts` (user did not request in final pass).

---

## Framing (use in bullets)

- **Context:** USF group capstone — full-stack ML system built from the ground up.
- **Ownership:** Brandon owned **system design** and the **FastAPI inference API** most heavily.
- **Credit:** Frame as **his project with collaborators** (do not list teammate names on the resume).
- **Emphasize:**
  1. **KNN-derived baselines** supporting **LightGBM** inference on sparse / non-metered blocks.
  2. **Automated model CI/CD** on GCP: **GitHub Actions** retrain pipeline, **MLflow** experiment tracking, **champion/challenger** promotion gated on validation MAE (`dev/promote_decision.py`).
  3. **Split deployment + CI:** **Vercel** (Next.js frontend), **Google Cloud Run** (FastAPI API), **GCS** (model artifacts); PR/push CI via `jobs.yml`; scheduled retrain every 3 days via `nightly-refresh.yml`.
- **Resume bullets:** No URLs or `\href{}` in Parkcast bullets (portfolio link is in resume header).
- **Roles to update:** `ai-engineer`, `forward-deployed-engineer`, and `MASTER-RESUME.tex` only. Leave `data-scientist` Parkcast selection unchanged unless IDs break after master edit.

---

## Step 1 — `GITHUB_README.md`

**Location:** Featured Projects table, **Parkcast-SF** row (currently repo link only).

**Change the Links column from:**

```markdown
| [Repo](https://github.com/Brandonminer333/ParkCast-SF) |
```

**To:**

```markdown
| [Live](https://parkcast-sf.vercel.app/) · [Repo](https://github.com/Brandonminer333/ParkCast-SF) |
```

Do **not** move Parkcast into "What I'm Building Now" or rewrite the description.

Pushing to `main` triggers `.github/workflows/sync-profile-readme.yml`, which syncs `GITHUB_README.md` → `Brandonminer333/Brandonminer333` profile README.

---

## Step 2 — `resume/latex/MASTER-RESUME.tex`

Follow `.cursor/skills/resume-builder/SKILL.md`.

Find the `\resumeProjectHeading` block:

```latex
{\textbf{Parkcast-SF: San Francisco Parking Assistant}}{}
```

### Replace active bullets with these three

```latex
            \resumeItem{Owned system design and the production \textbf{FastAPI} inference API for a full-stack USF capstone parking platform, serving block-level \textbf{LightGBM} occupancy predictions from artifacts in \textbf{GCS}. \textit{[ai-engineer, forward-deployed-engineer]}}

            \resumeItem{Applied \textbf{K-nearest neighbors}-derived baselines to support \textbf{LightGBM} inference on sparse, non-metered blocks where direct sensor history was unavailable. \textit{[ai-engineer, forward-deployed-engineer]}}

            \resumeItem{Built automated model \textbf{CI/CD} on \textbf{GCP}: scheduled \textbf{GitHub Actions} retraining with \textbf{MLflow} experiment tracking and a \textbf{champion/challenger} promotion gate (validation MAE) before GCS upload; split deploy across \textbf{Vercel} (frontend) and \textbf{Cloud Run} (API). \textit{[ai-engineer, forward-deployed-engineer]}}
```

### Retire superseded bullets

Mark the **previous** Parkcast bullets as `[..., commented]` (or remove them). These are outdated or folded into the three above:

- "Designed the full system architecture…" (generic; superseded by API ownership bullet)
- "Built a Vercel frontend…" (folded into CI/CD bullet)
- "Predicted parking availability… KNN approximation" (superseded by KNN + LightGBM bullet)
- "challenger deployed every 3 days" (superseded by CI/CD + MLflow bullet; note: cron is still every 3 days in `nightly-refresh.yml`)
- "OpenStreetMap and Mapbox" (not accurate for shipped frontend — Leaflet + OSRM)
- "Automated data ingestion into GCS…" (folded into CI/CD bullet)
- "separate Cloud Run instance to host MLflow" (MLflow is on GCP VM; fold into CI/CD bullet)

Example retirement tag:

```latex
\textit{[ai-engineer, forward-deployed-engineer, commented]}
```

---

## Step 3 — Refresh inventory IDs

From repo root `~/career`:

```bash
python resume/scripts/resume_builder.py inventory --role ai-engineer | grep -i parkcast
```

Note the `id` values for the **three new active** Parkcast bullets (not `[commented]` variants). IDs look like:

`projects-parkcast-sf-san-francisco-parking-assistant-N`

If you replaced bullets in place at the top of the list, they may still be `-1`, `-2`, `-3` — **verify** after edit; renumbering depends on whether old bullets were kept below as commented.

---

## Step 4 — `resume/scripts/rebuild_canonical_resumes.py`

### `ai-engineer`

Replace the three existing Parkcast entries in `BUILDS["ai-engineer"]["items"]`:

```python
"projects-parkcast-sf-san-francisco-parking-assistant-1",
"projects-parkcast-sf-san-francisco-parking-assistant-4",
"projects-parkcast-sf-san-francisco-parking-assistant-7",
```

With the three verified active IDs from Step 3 (likely `-1`, `-2`, `-3` if structure unchanged).

### `forward-deployed-engineer`

**Add** the same three Parkcast IDs. This resume currently has **no** Parkcast section. Insert after the California Grape project lines:

```python
"projects-california-grape-etl-pipeline-dashboard-title-variants-below-7",
"projects-california-grape-etl-pipeline-dashboard-title-variants-below-10",
"projects-california-grape-etl-pipeline-dashboard-title-variants-below-16",
# ADD:
"projects-parkcast-sf-san-francisco-parking-assistant-1",  # verify IDs
"projects-parkcast-sf-san-francisco-parking-assistant-2",
"projects-parkcast-sf-san-francisco-parking-assistant-3",
```

### `data-scientist`

Leave as-is (`projects-parkcast-sf-san-francisco-parking-assistant-3` only) unless rebuild fails because that ID now points to a commented variant — if so, remove the Parkcast line from data-scientist `items`.

### Rebuild

```bash
python3 resume/scripts/rebuild_canonical_resumes.py
```

Optional PDFs (requires Docker):

```bash
resume/scripts/build-pdfs.sh
```

---

## Step 5 — Verify output

### `resume/latex/ai-engineer.tex`

Parkcast section should contain exactly these three bullets (no role tags, no URLs):

1. Owned system design + FastAPI + LightGBM + GCS  
2. KNN baselines + LightGBM for sparse blocks  
3. CI/CD + MLflow champion/challenger + Vercel/Cloud Run split  

### `resume/latex/forward-deployed-engineer.tex`

Same three Parkcast bullets, inserted in the Technical Projects section (after California Grape, before section end).

### `GITHUB_README.md`

Parkcast row shows: `[Live](https://parkcast-sf.vercel.app/) · [Repo](...)`

### Quality checklist (from resume-builder skill)

- [ ] No `\textit{[...]}` role tags left in generated `.tex`
- [ ] No duplicate-theme bullets elsewhere on the resume
- [ ] Parkcast bullets have no URLs
- [ ] `rebuild_canonical_resumes.py` `BUILDS` IDs match inventory
- [ ] Rebuild script ran without error

---

## Technical reference (ParkCast-SF codebase)

Use these files if bullet wording needs a sanity check:

| Topic | Path |
|-------|------|
| FastAPI inference API | `app/main.py` |
| KNN + LightGBM features | `app/features.py` (KNN-derived baselines for inferred blocks) |
| Champion/challenger gate | `dev/promote_decision.py` |
| Scheduled retrain CI | `.github/workflows/nightly-refresh.yml` (cron: every 3 days) |
| PR/push CI | `.github/workflows/jobs.yml` |
| Model metrics | `app/models/LightGBM.meta.json` (MAE ~9.1%, R² ~0.73 — optional; not required in bullets) |
| Frontend | `parkcast-frontend/` (Next.js, Leaflet) |

---

## Agent execution checklist

```
- [ ] Read .cursor/skills/resume-builder/SKILL.md
- [ ] Edit GITHUB_README.md (Live link only)
- [ ] Edit MASTER-RESUME.tex Parkcast section
- [ ] Run inventory; confirm Parkcast bullet IDs
- [ ] Update rebuild_canonical_resumes.py (ai-engineer + forward-deployed-engineer)
- [ ] Run python3 resume/scripts/rebuild_canonical_resumes.py
- [ ] Spot-check ai-engineer.tex and forward-deployed-engineer.tex
- [ ] Do not commit unless user asks
```
